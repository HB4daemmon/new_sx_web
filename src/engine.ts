/** Pure deterministic rules. No DOM, clock, storage, or content IDs outside the supplied pack. */
export type Stat = 'hp'|'attack'|'defense'|'speed'|'hit'|'dodge'|'luck';
export type Slot = 'basic'|'rage'|'aux'|'artifact'|'strategy';
export type Stats = Record<Stat,number>;
export type Num = number | number[];
export interface Condition { type:string; target?:string; value?:number; key?:string; status?:string; tag?:string; stat?:Stat; text?:string; }
export interface Effect { type:string; target?:string; value?:any; coefficient?:Num; stat?:Stat; maxHpPercent?:number; percent?:Num; status?:string; pure?:boolean; key?:string; resource?:string; resourceTarget?:string; cap?:number; }
export interface Trigger { on:string; effects:Effect[]; once?:boolean; chance?:number; conditions?:Condition[]; }
export type SchoolId = 'rage'|'burn'|'guard'|'break'|'crit'|'evade'|'vitality'|'curse';
export interface School {id:SchoolId;name:string;tags:string[];one:string;two:string;finisher:{name:string;effect:string};}
export interface Hybrid {id:string;name:string;schools:[SchoolId,SchoolId];one:string;two:string;}
export interface RunPool {schools:SchoolId[];hybrids:string[];}
export interface Ability { schools?:SchoolId[];hybrid?:string; id:string; name:string; slot:Slot; tags:string[]; art:string; description:string; effects:Effect[]; triggers:Trigger[]; stats:Partial<Record<Stat,Num>>; rarity:string; cost:number; cooldown?:number; priority?:number; mechanicRole?:'starter'|'generator'|'converter'|'amplifier'|'bridge'|'keystone'|'risk'|'payoff'; }
export interface Origin { id:string; name:string; subtitle:string; description:string; art:string; stats:Stats; affinity:string; starting:string[]; battleStartEffects?:Effect[]; }
export interface Fate {resonanceBias?:SchoolId[]; id:string; name:string; art:string; description:string; stats?:Partial<Stats>; percent?:Partial<Stats>; triggers?:Trigger[]; damageByRound?:number[]; }
export interface BattleObjective { type:'kill'|'survive'|'break'; rounds?:number; value?:number; label?:string; }
export interface BossPhase { hpPercent:number; label:string; effects?:Effect[]; }
export interface Enemy { id:string; name:string; act:number; kind:string; art:string; stats:Stats; abilities:string[]; tags:string[]; intent:string; ai?:'greedy'|'weighted'; objective?:BattleObjective; phases?:BossPhase[]; }
export interface EventBranch { text:string; effects:Effect[]; grant?:{id?:string;slot?:Slot}|null; next?:string; }
export interface EventCheck { stat:Stat; base:number; difficulty:number; success:EventBranch; failure:EventBranch; }
export interface Choice { id:string; label:string; text:string; preview?:string; effects:Effect[]; grant?:{id?:string;slot?:Slot}|null; conditions:Condition[]; next?:string; check?:EventCheck; tone?:'safe'|'cost'|'check'|'fate'; consequence?:string; }
export interface EventPhase { id:string; text:string; choices:Choice[]; }
export interface Story {requirements?:Condition[];starts?:{phase:string;weight:number;conditions?:Condition[]}[];id:string;act:number;major:boolean;name:string;npc:string;art:string;text:string;choices:Choice[];phases?:EventPhase[];startPhase?:string;profile?:'A'|'B';weight?:number;maxPerRun?:number;category?:'encounter'|'bargain'|'trial'|'echo'|'secret'|'major';hiddenOnly?:boolean;relevance?:Condition[];character?:string;episode?:number;episodes?:number;relationKey?:string;}
export interface Ending { id:string; name:string; art:string; description:string; conditions:Condition[]; }
export interface Content {schools:School[];hybrids:Hybrid[]; version:string;rulesVersion:string;title:string;subtitle:string;abilities:Ability[];origins:Origin[];fates:Fate[];enemies:Enemy[];events:Story[];acts:{name:string;subtitle:string;chapter:string;boss:string;major:string;tint:string;bosses?:string[]}[];endings:Ending[];realms:string[];rules:{routeRows:string[][];actStartFullHeal:boolean;poolSchools:number;poolHybrids:number;xpThresholds:number[];levelStats:Partial<Stats>;rageMax:number;basicRage:number;hitRage:number;maxRounds:number;burnDamage:number;weakPercent:number;breakDefense:number;vulnerablePercent:number;startingGold:number;refreshPrices:number[];restPercent:number;rewards:Record<string,{gold:number;xp:number}>};labels:Record<string,Record<string,string>>; }
export interface Equipped { id:string; rank:number; }
export interface RouteNode {id:string;act:number;row:number;lane:number;type:string;ref?:string;}
export type Phase = 'map'|'battle'|'reward'|'shop'|'rest'|'event'|'event_result'|'ending'|'finished'|'dead';
export interface Fighter {name:string;stats:Stats;hp:number;rage:number;shield:number;status:Record<string,number>;abilities:Equipped[];fate?:Fate;used:string[];tags:string[];cooldown:Record<string,number>;phase:number;}
export interface CombatBreakdown { base:number; scaled:number; afterDefense:number; afterVulnerable:number; afterBuild:number; afterCrit:number; absorbed:number; hpLoss:number; }
export interface BuildResonance {id:SchoolId;name:string;tier:0|1|2;count:number;next:number;description:string;}
export interface BuildBridge {id:string;name:string;schools:[BuildResonance['id'],BuildResonance['id']];tier:0|1|2;counts:[number,number];description:string;}
export interface BuildFinisher {id:BuildResonance['id']|'none';name:string;active:boolean;progress:number;description:string;requirement:string;}
export type AbilityBuildRole='starter'|'core'|'amplifier'|'bridge'|'finisher';
export interface AbilityRoleInfo {role:AbilityBuildRole;label:string;reason:string;school:BuildResonance['id']|'none';fit:number;}
export interface BuildProfile {tags:Record<string,number>;resonances:BuildResonance[];dominant:string[];core:BuildResonance['id']|'none';bridge:BuildBridge;finisher:BuildFinisher;strategy:{name:string;count:number;tier:0|1|2;bonusPercent:number;description:string};score:number;}
export interface Frame {round:number;actor:'p'|'e'|'system';kind:string;label:string;amount:number;crit?:boolean;p:Fighter;e:Fighter;sourceId?:string;detail?:string;breakdown?:CombatBreakdown;objective?:string;phase?:number;}
export interface BattleSummary {playerDamage:number;enemyDamage:number;playerShieldAbsorbed:number;enemyShieldAbsorbed:number;playerCrits:number;enemyCrits:number;playerMisses:number;enemyMisses:number;playerRageSkills:number;enemyRageSkills:number;playerConversions:number;enemyConversions:number;statusTicks:number;phaseChanges:number;}
export interface Battle {enemyId:string;frames:Frame[];won:boolean;rounds:number;hp:number;cursor:number;objective:BattleObjective;summary:BattleSummary;objectiveProgress:number;}
export interface Stock {kind:'ability'|'heal'|'stat';id?:string;price:number;sold:boolean;stat?:Stat;value?:number;}
export interface Pending {id:string;source:'reward'|'shop'|'event';price:number;index?:number;}
export interface EventCheckResult {eventId:string;phase:string;choiceId:string;stat:Stat;chance:number;roll:number;success:boolean;}
export interface StoryLogItem {act:number;title:string;choice:string;phase?:string;result?:string;chance?:number;roll?:number;success?:boolean;eventId?:string;choiceId?:string;category?:string;consequence?:string;}
export interface RunState {contentPool:RunPool;version:string;rulesVersion:string;seed:string;name:string;portrait:string;origin:string;fate:string;act:number;row:number;lane:number;phase:Phase;hp:number;gold:number;xp:number;level:number;bonus:Partial<Stats>;slots:(Equipped|null)[];rng:Record<string,number>;route:RouteNode[];visited:string[];facts:Record<string,boolean>;counters:Record<string,number>;threads:Record<string,string>;seenEvents:Record<string,number>;refreshes:number[];reward:string[];stock:Stock[];temper:Stat[];current?:RouteNode;battle?:Battle;pending?:Pending;eventId?:string;eventPhase?:string;eventStep?:number;lastCheck?:EventCheckResult;resultText?:string;endingId?:string;actionLog:{command:string;args:any[]}[];storyLog:StoryLogItem[];rewardInfo?:{gold:number;xp:number};}

export const SLOT_ORDER:Slot[]=['basic','rage','aux','aux','artifact','artifact','artifact','strategy'];
export const STAT_KEYS:Stat[]=['hp','attack','defense','speed','hit','dodge','luck'];
export const EFFECT_TYPES=['damage','resource_damage','heal','gain_shield','gain_rage','lose_rage','consume_status','consume_shield','apply_status','remove_status','modify_stat','gain_currency','gain_xp','rank_up','set_fact','add_counter','advance_thread'];
export const TRIGGER_TYPES=['battle_start','before_action','after_action','on_hit','on_crit','on_evade','on_heal','on_damaged','on_rage_skill','hp_threshold','round_end','battle_end'];
export const CONDITION_TYPES=['hp_below','hp_above','has_status','status_stack_at_least','rage_at_least','has_fact','counter_at_least','act_is','enemy_tag','player_tag','currency_at_least','hp_above_absolute','thread_is','event_seen','stat_at_least'];
export const clone=<T>(x:T):T=>JSON.parse(JSON.stringify(x));
export const value=(x:Num|undefined,r=0):number=>Math.trunc(Array.isArray(x)?x[Math.min(r,x.length-1)]:(x??0));
export const clamp=(x:number,min:number,max:number)=>Math.max(min,Math.min(max,x));
export function hash(text:string):number {let x=2166136261;for(let i=0;i<text.length;i++){x^=text.charCodeAt(i);x=Math.imul(x,16777619);}return (x>>>0)||1;}
export function random(s:{seed:string;rng:Record<string,number>},stream:string):number {let x=s.rng[stream]??hash(s.seed+'|'+stream);x^=x<<13;x^=x>>>17;x^=x<<5;s.rng[stream]=x>>>0;return (x>>>0)/4294967296;}
export function pick<T>(s:{seed:string;rng:Record<string,number>},stream:string,a:T[]):T {if(!a.length)throw new Error('Empty choice pool');return a[Math.floor(random(s,stream)*a.length)];}
export function sample<T>(s:{seed:string;rng:Record<string,number>},stream:string,a:T[],n:number):T[]{const pool=[...a],out:T[]=[];while(pool.length&&out.length<n)out.push(pool.splice(Math.floor(random(s,stream)*pool.length),1)[0]);return out;}
export function fateOptions(c:Content,seed:string):Fate[]{return sample({seed,rng:{}},'fate',c.fates,3);}

function weighted<T>(s:{seed:string;rng:Record<string,number>},stream:string,items:T[],weight:(x:T)=>number):T {
 if(!items.length)throw new Error('Empty weighted pool');const weights=items.map(x=>Math.max(0,weight(x))),total=weights.reduce((a,b)=>a+b,0);if(total<=0)return items[0];
 let cursor=random(s,stream)*total;for(let i=0;i<items.length;i++){if(cursor<weights[i])return items[i];cursor-=weights[i];}return items[items.length-1];
}

export function validateContent(c:Content):void {
 if(!c||!c.version||!Array.isArray(c.abilities))throw new Error('Invalid content pack');
 const schoolIds=new Set(c.schools?.map(x=>x.id)),hybridIds=new Set(c.hybrids?.map(x=>x.id));
 if(schoolIds.size!==c.schools?.length||!schoolIds.size||hybridIds.size!==c.hybrids?.length)throw new Error('Invalid school registry');
 for(const h of c.hybrids)if(h.schools.length!==2||h.schools[0]===h.schools[1]||h.schools.some(id=>!schoolIds.has(id)))throw new Error('Broken hybrid');
 if(c.rules.poolSchools<2||c.rules.poolSchools>schoolIds.size||c.rules.poolHybrids<1||c.rules.poolHybrids>hybridIds.size)throw new Error('Invalid run pool size');
 if(!c.rules.routeRows?.length||c.rules.routeRows.at(-1)?.join()!=='boss')throw new Error('Invalid route rows');
 for(const row of c.rules.routeRows)if(![1,3].includes(row.length)||row.some(t=>!['combat','elite','event','hidden','shop','rest','major','treasure','boss'].includes(t)))throw new Error('Invalid route node');
 const ids=new Set<string>();for(const list of [c.abilities,c.origins,c.fates,c.enemies,c.events,c.endings])for(const entity of list){if(!entity.id||ids.has(entity.id))throw new Error('Duplicate or missing ID: '+entity.id);ids.add(entity.id);}
 const abilities=new Set(c.abilities.map(x=>x.id));
 const checkConditions=(a:Condition[])=>{for(const x of a){if(!CONDITION_TYPES.includes(x.type))throw new Error('Unknown condition '+x.type);if(x.stat&&!STAT_KEYS.includes(x.stat))throw new Error('Unknown condition stat');}};
 const checkEffects=(a:Effect[])=>{for(const e of a){if(!EFFECT_TYPES.includes(e.type))throw new Error('Unknown effect '+e.type);if(e.coefficient!==undefined&&[e.coefficient].flat().some(x=>!Number.isFinite(x)||x<0||x>2000))throw new Error('Unsafe coefficient');if(e.stat&&!STAT_KEYS.includes(e.stat))throw new Error('Unknown stat');if(['apply_status','remove_status','consume_status'].includes(e.type)&&!['burn','weak','break','stun','vulnerable'].includes(e.status??''))throw new Error('Unknown status effect');if(e.type==='resource_damage'&&!['burn','break','weak','vulnerable','shield','rage'].includes(e.resource??''))throw new Error('Unknown combat resource');if(e.type==='resource_damage'&&e.resourceTarget&&!['self','enemy'].includes(e.resourceTarget))throw new Error('Unknown resource target');}};
 const checkGrant=(g:{id?:string;slot?:Slot}|null|undefined)=>{if(g?.id&&!abilities.has(g.id))throw new Error('Broken grant');if(g?.slot&&!SLOT_ORDER.includes(g.slot))throw new Error('Broken grant slot');};
 const checkChoice=(choice:Choice,phaseIds?:Set<string>)=>{checkEffects(choice.effects??[]);checkConditions(choice.conditions??[]);checkGrant(choice.grant);if(choice.next&&phaseIds&&!phaseIds.has(choice.next))throw new Error('Broken event phase '+choice.next);if(choice.check){if(!STAT_KEYS.includes(choice.check.stat)||!Number.isFinite(choice.check.base)||!Number.isFinite(choice.check.difficulty))throw new Error('Invalid event check');for(const branch of [choice.check.success,choice.check.failure]){checkEffects(branch.effects??[]);checkGrant(branch.grant);if(branch.next&&phaseIds&&!phaseIds.has(branch.next))throw new Error('Broken event branch '+branch.next);}}};
 for(const a of c.abilities){if(a.schools?.some(id=>!schoolIds.has(id))||(a.hybrid&&!hybridIds.has(a.hybrid)))throw new Error('Broken ability school');if(!SLOT_ORDER.includes(a.slot))throw new Error('Invalid slot');if((a.cooldown??0)<0||(a.cooldown??0)>20)throw new Error('Invalid cooldown');checkEffects(a.effects);for(const t of a.triggers){if(!TRIGGER_TYPES.includes(t.on))throw new Error('Unknown trigger');checkEffects(t.effects);checkConditions(t.conditions??[]);}}
 for(const f of c.fates){for(const id of f.resonanceBias??[])if(!c.schools.some(x=>x.id===id))throw new Error('Unknown fate resonance '+id);for(const t of f.triggers??[]){checkEffects(t.effects);checkConditions(t.conditions??[]);}}
 for(const x of [...c.origins.map(x=>({ids:x.starting,stats:x.stats})),...c.enemies.map(x=>({ids:x.abilities,stats:x.stats}))]){for(const id of x.ids)if(!abilities.has(id))throw new Error('Broken ability reference '+id);for(const key of STAT_KEYS)if(!Number.isInteger(x.stats[key])||x.stats[key]<0||x.stats[key]>10000)throw new Error('Invalid stat '+key);if(x.stats.hp<1)throw new Error('Invalid HP');}
 for(const enemy of c.enemies){if(enemy.objective&&!['kill','survive','break'].includes(enemy.objective.type))throw new Error('Invalid objective');for(const p of enemy.phases??[]){if(p.hpPercent<=0||p.hpPercent>=100)throw new Error('Invalid boss phase');checkEffects(p.effects??[]);}}
 for(const e of c.events){checkConditions(e.relevance??[]);checkConditions(e.requirements??[]);if((e.phases?.length??0)>5)throw new Error('Too many event phases');const phaseIds=new Set((e.phases??[]).map(p=>p.id));if(e.phases?.length&&phaseIds.size!==e.phases.length)throw new Error('Duplicate event phase');for(const entry of e.starts??[]){if(!phaseIds.has(entry.phase)||entry.weight<=0)throw new Error('Broken random event start');checkConditions(entry.conditions??[]);}if(e.startPhase&& !phaseIds.has(e.startPhase))throw new Error('Broken start phase');for(const choice of e.choices??[])checkChoice(choice,phaseIds);for(const p of e.phases??[]){if(p.choices.length<2||p.choices.length>4)throw new Error('Event phase must have 2-4 choices');for(const choice of p.choices)checkChoice(choice,phaseIds);}}
 for(const a of c.acts){if(a.bosses?.some(id=>!c.enemies.some(e=>e.id===id&&e.kind==='boss')))throw new Error('Broken boss pool');if(!c.enemies.some(e=>e.id===a.boss)||!c.events.some(e=>e.id===a.major))throw new Error('Broken act reference');}
 for(const e of c.endings)checkConditions(e.conditions);
}

export function playerStats(c:Content,s:RunState):Stats {
 const o=c.origins.find(x=>x.id===s.origin)!;const f=c.fates.find(x=>x.id===s.fate)!;const r={...o.stats};
 for(const k of STAT_KEYS)r[k]+=value(s.bonus[k])+value(f.stats?.[k])+value(c.rules.levelStats[k])*s.level;
 for(const eq of s.slots)if(eq){const a=c.abilities.find(x=>x.id===eq.id)!;for(const k of STAT_KEYS)r[k]+=value(a.stats[k],eq.rank);}
 for(const k of STAT_KEYS)r[k]=Math.max(k==='hp'?1:0,Math.floor(r[k]*(100+value(f.percent?.[k]))/100));return r;
}

/** Gameplay V5 build profile: a core school, one deterministic cross-school bridge, and a late-run finisher. */
export function buildProfile(c:Content,s:RunState):BuildProfile {
 const equipped=s.slots.filter((x):x is Equipped=>!!x).map(eq=>({eq,a:c.abilities.find(x=>x.id===eq.id)!}));
 const tags:Record<string,number>={};for(const {a} of equipped)for(const tag of a.tags)tags[tag]=(tags[tag]??0)+1;
 const fate=c.fates.find(x=>x.id===s.fate)!,fateBias=fate.resonanceBias??[];const school=(id:BuildResonance['id'],name:string,match:string[],tier1:string,tier2:string):BuildResonance=>{const biased=fateBias.includes(id),count=equipped.filter(({a})=>match.some(t=>a.tags.includes(t))).length+(biased?1:0);const tier=(count>=4?2:count>=2?1:0) as 0|1|2,hint=biased?' · 命格引路 +1':'';return {id,name,tier,count,next:tier===0?2:4,description:(tier===2?tier2:tier===1?tier1:`再有 ${Math.max(0,2-count)} 项同源能力可激活`)+hint};};
 const resonances=c.schools.map(d=>school(d.id,d.name+'共鸣',d.tags,d.one,d.two));
 const strategyEq=s.slots.find((eq,i)=>eq&&SLOT_ORDER[i]==='strategy'),strategyAbility=strategyEq?c.abilities.find(a=>a.id===strategyEq.id):undefined;
 const aligned=strategyAbility?equipped.filter(({a})=>a.id!==strategyAbility.id&&a.tags.some(t=>strategyAbility.tags.includes(t))).length:0,strategyTier=(aligned>=5?2:aligned>=3?1:0) as 0|1|2,bonusPercent=strategyTier===2?18:strategyTier===1?10:0;
 const order:SchoolId[]=c.schools.map(d=>d.id);const ranked=[...resonances].sort((a,b)=>b.tier-a.tier||b.count-a.count||order.indexOf(a.id)-order.indexOf(b.id)),core=(ranked[0]?.count??0)>0?ranked[0].id:'none';
 const defs=c.hybrids.filter(d=>s.contentPool.hybrids.includes(d.id));
 const pairs=defs.map((d,index)=>{const a=resonances.find(x=>x.id===d.schools[0])!,b=resonances.find(x=>x.id===d.schools[1])!,tier=(a.tier>=2&&b.tier>=2?2:a.tier>=1&&b.tier>=1?1:0) as 0|1|2;return {...d,index,tier,counts:[a.count,b.count] as [number,number]};}).sort((a,b)=>b.tier-a.tier||Math.min(...b.counts)-Math.min(...a.counts)||(b.counts[0]+b.counts[1])-(a.counts[0]+a.counts[1])||a.index-b.index);
 const best=pairs[0]!,left=resonances.find(x=>x.id===best.schools[0])!,right=resonances.find(x=>x.id===best.schools[1])!,bridge:BuildBridge={id:best.id,name:best.name,schools:best.schools,tier:best.tier,counts:best.counts,description:best.tier===2?best.two:best.tier===1?best.one:`${left.name} ${left.count}/2 · ${right.name} ${right.count}/2；双系各达一重后激活`};
 const coreRes=core==='none'?undefined:resonances.find(x=>x.id===core),finisherDef=c.schools.find(d=>d.id===core)?.finisher;
 const progress=(coreRes?.tier===2?1:0)+(strategyTier===2?1:0)+(equipped.length===8?1:0),finisher:BuildFinisher={id:core,name:finisherDef?.name??'未定法相',active:progress===3&&core!=='none',progress,description:progress===3&&finisherDef?finisherDef.effect:'需要主轴二重、战策二重与八槽全满',requirement:'主轴二重 · 战策二重 · 八槽全满'};
 const dominant=Object.entries(tags).sort((a,b)=>b[1]-a[1]||a[0].localeCompare(b[0])).slice(0,4).map(([k])=>k),ranks=equipped.reduce((n,{eq})=>n+eq.rank,0),score=equipped.length*4+ranks*3+resonances.reduce((n,r)=>n+r.tier*10,0)+strategyTier*8+bridge.tier*12+progress*4+(finisher.active?15:0);
 return {tags,resonances,dominant,core,bridge,finisher,strategy:{name:strategyAbility?.name??'未装配战策',count:aligned,tier:strategyTier,bonusPercent,description:strategyTier?`战策与 ${aligned} 项能力同源，全伤害 +${bonusPercent}%`:`战策与至少 3 项能力共享标签后激活统摄`},score};
}

const schoolTags=(c:Content,id:SchoolId)=>c.schools.find(d=>d.id===id)!.tags;
const schoolName=(c:Content,id:SchoolId)=>c.schools.find(d=>d.id===id)!.name;
const BUILD_ROLE_LABELS:Record<AbilityBuildRole,string>={starter:'启动件',core:'核心件',amplifier:'放大件',bridge:'桥接件',finisher:'终局件'};

/** Contextual role of an ability inside the current board. The same ability may change role after the board pivots. */
export function abilityRole(c:Content,s:RunState,a:Ability):AbilityRoleInfo {
 const b=buildProfile(c,s),schoolScores=c.schools.map(d=>d.id).map(id=>({id,n:a.tags.filter(t=>schoolTags(c,id).includes(t)).length})).sort((x,y)=>y.n-x.n||x.id.localeCompare(y.id)),school=schoolScores[0]?.n?schoolScores[0].id:'none';
 const coreTags=b.core==='none'?[]:schoolTags(c,b.core),coreMatch=a.tags.some(t=>coreTags.includes(t)),weakIndex=b.bridge.counts[0]<=b.bridge.counts[1]?0:1,weakSchool=b.bridge.schools[weakIndex],weakTags=schoolTags(c,weakSchool),weakMatch=a.tags.some(t=>weakTags.includes(t)),slotHasRoom=SLOT_ORDER.some((slot,i)=>slot===a.slot&&!s.slots[i]),coreRes=b.core==='none'?undefined:b.resonances.find(x=>x.id===b.core),wouldCompleteCore=!!coreRes&&coreRes.count===3&&coreMatch;
 const triggerOns=new Set((a.triggers??[]).map(t=>t.on)),effectTypes=new Set((a.effects??[]).map(e=>e.type));let role:AbilityBuildRole,reason:string;
 if(a.slot==='strategy'||(b.finisher.progress>=2&&slotHasRoom&&coreMatch)||(wouldCompleteCore&&b.strategy.tier>=1)){role='finisher';reason=a.slot==='strategy'?'战策决定八槽的统摄方向，是终局法相的关键条件。':'这项能力能直接推进主轴或八槽终局条件。';}
 else if(a.slot==='basic'||triggerOns.has('battle_start')){role='starter';reason='负责稳定起手和基础循环，让构筑每场战斗都能可靠启动。';}
 else if(b.bridge.tier<2&&weakMatch&&(weakSchool!==b.core||!coreMatch)){role='bridge';reason=`补强 ${schoolName(c,weakSchool)} 一侧，推进「${b.bridge.name}」。`; }
 else if(a.slot==='artifact'||(a.triggers??[]).length>0||effectTypes.has('modify_stat')){role='amplifier';reason=coreMatch?'放大当前主轴已有的资源、状态或触发收益。':'提供被动放大与容错，适合补足现有循环。';}
 else {role='core';reason=coreMatch?'直接服务当前主轴，是主要输出或资源循环的一部分。':'可作为新的构筑核心，适合主动转型。';}
 const fit=(coreMatch?3:0)+(weakMatch&&b.bridge.tier<2?2:0)+(role==='finisher'?3:role==='bridge'?2:role==='core'?2:role==='amplifier'?1:0)+(a.tags.reduce((n,t)=>n+Math.min(2,b.tags[t]??0),0));
 return {role,label:BUILD_ROLE_LABELS[role],reason,school,fit};
}

function fighter(c:Content,s:RunState,enemy?:Enemy):Fighter {const stats=enemy?{...enemy.stats}:playerStats(c,s);return {name:enemy?.name??s.name,stats,hp:enemy?stats.hp:Math.min(s.hp,stats.hp),rage:0,shield:0,status:{},abilities:enemy?enemy.abilities.map(id=>({id,rank:0})):clone(s.slots.filter((x):x is Equipped=>!!x)),fate:enemy?undefined:c.fates.find(x=>x.id===s.fate),used:[],tags:enemy?.tags??[c.origins.find(x=>x.id===s.origin)!.affinity],cooldown:{},phase:0};}

export function conditionOK(c:Content,s:RunState,q:Condition,self?:Fighter,enemy?:Fighter):boolean {
 const who=q.target==='enemy'?enemy:self;const hp=who?.hp??s.hp,max=who?.stats.hp??playerStats(c,s).hp;const n=q.status==='shield'?(who?.shield??0):(who?.status[q.status??'']??0);
 switch(q.type){case 'hp_below':return hp*100<max*(q.value??0);case 'hp_above':return hp*100>max*(q.value??0);case 'hp_above_absolute':return hp>(q.value??0);case 'has_status':return n>0;case 'status_stack_at_least':return n>=(q.value??1);case 'rage_at_least':return (who?.rage??0)>=(q.value??0);case 'has_fact':return !!s.facts[q.key??''];case 'counter_at_least':return (s.counters[q.key??'']??0)>=(q.value??0);case 'currency_at_least':return s.gold>=(q.value??0);case 'act_is':return s.act===(q.value??0);case 'enemy_tag':return !!enemy?.tags.includes(q.tag??'');case 'player_tag':return !!self?.tags.includes(q.tag??'');case 'thread_is':return s.threads[q.key??'']===q.text;case 'event_seen':return (s.seenEvents[q.key??'']??0)>=(q.value??1);case 'stat_at_least':return playerStats(c,s)[q.stat??'luck']>=(q.value??0);default:throw new Error('Unsupported condition: '+q.type);}
}

/** Resolve once, then replay snapshots. Playback speed can never alter the result. */
export function simulateBattle(c:Content,s:RunState,enemy:Enemy):Battle {
 const p=fighter(c,s),e=fighter(c,s,enemy),frames:Frame[]=[];let round=0,eventBudget=0,objectiveProgress=0,objectiveWon=false;
 const build=buildProfile(c,s),res=(id:BuildResonance['id'])=>build.resonances.find(x=>x.id===id)!.tier,rageTier=res('rage'),burnTier=res('burn'),guardTier=res('guard'),breakTier=res('break'),bridge=build.bridge,finisher=build.finisher;let finisherUsed=false,guardBreakRound=-1;const critTier=res('crit'),evadeTier=res('evade'),vitalityTier=res('vitality'),curseTier=res('curse');
 const bridgeTier=(id:BuildBridge['id'])=>bridge.id===id?bridge.tier:0;
 const objective:BattleObjective=clone(enemy.objective??{type:'kill',label:'击败敌人'});const summary:BattleSummary={playerDamage:0,enemyDamage:0,playerShieldAbsorbed:0,enemyShieldAbsorbed:0,playerCrits:0,enemyCrits:0,playerMisses:0,enemyMisses:0,playerRageSkills:0,enemyRageSkills:0,playerConversions:0,enemyConversions:0,statusTicks:0,phaseChanges:0};
 const side=(f:Fighter)=>f===p?'p':f===e?'e':'system';
 const frame=(actor:Fighter|undefined,kind:string,label:string,amount=0,crit=false,meta:Partial<Frame>={})=>frames.push({round,actor:actor?side(actor):'system',kind,label,amount,crit,p:clone(p),e:clone(e),...meta});
 const stat=(f:Fighter,k:Stat)=>k==='attack'?Math.max(1,Math.floor(f.stats.attack*(f.status.weak?100-c.rules.weakPercent:100)/100)):k==='defense'?Math.max(0,f.stats.defense-(f.status.break??0)*c.rules.breakDefense):f.stats[k];
 const changeRage=(f:Fighter,n:number)=>{f.rage=clamp(f.rage+n,0,c.rules.rageMax);};
 const stream=(f:Fighter,kind:string)=>`combat.${f===p?'p':'e'}.${kind}`;
 function hurt(a:Fighter,b:Fighter,n:number,label:string,crit=false,breakdown?:CombatBreakdown,sourceId?:string){
  n=Math.max(1,Math.floor(n));const shieldBefore=b.shield,absorbed=Math.min(b.shield,n);b.shield-=absorbed;const hpLoss=Math.max(0,n-absorbed);b.hp=Math.max(0,b.hp-hpLoss);let guardFinisher=false;
  if(b===p&&a===e&&absorbed>0){const gb=bridgeTier('guard_break');if(gb>0&&guardBreakRound!==round){guardBreakRound=round;a.status.break=clamp((a.status.break??0)+gb,0,20);}guardFinisher=finisher.active&&finisher.id==='guard'&&!finisherUsed&&shieldBefore>0&&b.shield===0;}
  if(b===p){summary.enemyDamage+=hpLoss;summary.playerShieldAbsorbed+=absorbed;}else{summary.playerDamage+=hpLoss;summary.enemyShieldAbsorbed+=absorbed;if(a===p)objectiveProgress+=hpLoss+absorbed;}if(breakdown){breakdown.absorbed=absorbed;breakdown.hpLoss=hpLoss;}
  frame(a,'damage',label,hpLoss,crit,{sourceId,breakdown,detail:absorbed?`护盾吸收 ${absorbed}`:undefined});
  if(guardFinisher&&a.hp>0){finisherUsed=true;const rebound=Math.max(1,Math.floor(stat(p,'defense')*160/100)),reforge=Math.max(1,Math.floor(stat(p,'defense')*50/100));p.shield=Math.min(9999,p.shield+reforge);hurt(p,a,rebound,'玄武返照',false,{base:rebound,scaled:rebound,afterDefense:rebound,afterVulnerable:rebound,afterBuild:rebound,afterCrit:rebound,absorbed:0,hpLoss:0},'build.finisher.guard');frame(undefined,'build','终局法相 · 玄武返照',0,false,{detail:`护盾破碎，反震 ${rebound} 并重铸 ${reforge} 护盾`});}
  if(objective.type==='break'&&objectiveProgress>=(objective.value??1))objectiveWon=true;if(b===e)checkBossPhase();
 }
 function apply(a:Fighter,b:Fighter,ef:Effect,rank:number,label:string,secondary=false,sourceId?:string){if(++eventBudget>768)throw new Error('Combat effect budget exceeded');const t=ef.target==='self'?a:b;let n=value(ef.value,rank);switch(ef.type){case 'damage':{let base=n;let scaled=base+Math.floor(stat(a,ef.stat??'attack')*value(ef.coefficient,rank)/100)+Math.floor(t.stats.hp*(ef.maxHpPercent??0)/100);let afterDefense=ef.pure?Math.max(1,scaled):Math.max(1,scaled-stat(t,'defense'));let afterVulnerable=t.status.vulnerable?Math.floor(afterDefense*(100+c.rules.vulnerablePercent)/100):afterDefense;let buildPercent=100;if(a===p&&t!==a){buildPercent+=build.strategy.bonusPercent;if(curseTier>=2)buildPercent+=Math.min(15,((t.status.weak??0)+(t.status.vulnerable??0))*3);if(guardTier>=2&&a.shield>0)buildPercent+=10;if(breakTier>=2&&(t.status.break??0)>0)buildPercent+=20;if(bridgeTier('rage_break')>=2&&(t.status.break??0)>0&&c.abilities.find(x=>x.id===sourceId)?.slot==='rage')buildPercent+=15;}let afterBuild=Math.max(1,Math.floor(afterVulnerable*buildPercent/100));let crit=false;if(t!==a&&!secondary){const hit=clamp(9500+a.stats.hit*50-t.stats.dodge*50,6000,10000);if(random(s,stream(a,'hit'))*10000>=hit){if(a===p)summary.playerMisses++;else summary.enemyMisses++;frame(a,'miss',label,0,false,{sourceId,detail:`命中率 ${(hit/100).toFixed(1)}%`});if(t.hp>0){if(t===p&&evadeTier>=2)changeRage(p,10);if(t===p&&bridgeTier('crit_evade')>0){apply(p,e,{type:'modify_stat',target:'self',stat:'luck',value:bridgeTier('crit_evade')===2?4:2},0,'惊鸿夺势',true,'build.bridge.crit_evade');frame(undefined,'build','惊鸿夺势',0,false,{sourceId:'build.bridge.crit_evade',detail:'闪避之后，气运凝聚'});}if(t===p&&finisher.active&&finisher.id==='evade'&&!finisherUsed){finisherUsed=true;apply(p,e,{type:'damage',stat:'speed',coefficient:200,pure:true},0,'无影回锋',true,'build.finisher.evade');}trigger(t,a,'on_evade');}return false;}crit=random(s,stream(a,'crit'))*10000<Math.min(5000,500+a.stats.luck*30+(a===p&&critTier>=1?1000:0));if(crit){afterBuild=Math.floor(afterBuild*(a===p&&critTier>=2?175:150)/100);if(a===p)summary.playerCrits++;else summary.enemyCrits++;}}let afterCrit=afterBuild;if(a.fate?.damageByRound&&t!==a)afterCrit=Math.floor(afterCrit*a.fate.damageByRound[Math.min(Math.max(round-1,0),a.fate.damageByRound.length-1)]/100);const breakdown:CombatBreakdown={base,scaled,afterDefense,afterVulnerable,afterBuild,afterCrit,absorbed:0,hpLoss:0};hurt(a,t,afterCrit,label,crit,breakdown,sourceId);if(crit&&a.hp>0){if(a===p&&bridgeTier('crit_evade')>0)apply(p,e,{type:'gain_shield',target:'self',stat:'speed',coefficient:bridgeTier('crit_evade')===2?55:30},0,'惊鸿夺势',true,'build.bridge.crit_evade');if(a===p&&finisher.active&&finisher.id==='crit'&&!finisherUsed&&t.hp>0){finisherUsed=true;apply(p,t,{type:'damage',coefficient:100,pure:true},0,'星落断命',true,'build.finisher.crit');}trigger(a,t,'on_crit');}return true;}case 'resource_damage':{const holder=ef.resourceTarget==='self'?a:b,key=ef.resource!,raw=key==='shield'?holder.shield:key==='rage'?holder.rage:(holder.status[key]??0),units=Math.min(Math.max(0,raw),Math.max(0,ef.cap??9999)),per=value(ef.coefficient,rank);if(units<=0||per<=0)break;return apply(a,b,{type:'damage',target:ef.target,coefficient:units*per,stat:ef.stat??'attack',pure:ef.pure},rank,label,true,sourceId);}case 'heal':{const before=t.hp;const amount=n+Math.floor(t.stats.hp*(ef.maxHpPercent??0)/100);t.hp=Math.min(t.stats.hp,t.hp+Math.floor(amount*(t===p&&vitalityTier>=1?125:100)/100));const healed=t.hp-before;if(healed>0){frame(a,'heal',label,healed,false,{sourceId});if(t===p&&vitalityTier>=2)t.shield=Math.min(9999,t.shield+Math.floor(healed*.5));if(t===p&&bridgeTier('vitality_curse')>0&&e.hp>0)apply(p,e,{type:'apply_status',target:'enemy',status:'weak',value:bridgeTier('vitality_curse')===2?2:1},0,'枯荣同契',true,'build.bridge.vitality_curse');if(t===p&&finisher.active&&finisher.id==='vitality'&&!finisherUsed&&p.hp*100>=p.stats.hp*70){finisherUsed=true;apply(p,e,{type:'gain_shield',target:'self',value:Math.floor(p.stats.hp*.3)},0,'生生不息',true,'build.finisher.vitality');}trigger(t,t===p?e:p,'on_heal');}break;}case 'gain_shield':t.shield=Math.min(9999,t.shield+n+Math.floor(stat(a,ef.stat??'defense')*value(ef.coefficient,rank)/100));break;case 'gain_rage':changeRage(t,n);break;case 'lose_rage':changeRage(t,-n);break;case 'consume_status':{const key=ef.status!,before=t.status[key]??0,spent=Math.min(before,Math.max(0,n));t.status[key]=Math.max(0,before-spent);if(spent>0){if(a===p)summary.playerConversions++;else summary.enemyConversions++;const resource=key==='burn'?'燃烧':key==='break'?'破甲':key;frame(a,'convert',label,0,false,{sourceId,detail:`炼化 ${spent} 层${resource}`});}break;}case 'consume_shield':{const spent=Math.min(t.shield,Math.max(0,n));t.shield=Math.max(0,t.shield-spent);if(spent>0){if(a===p)summary.playerConversions++;else summary.enemyConversions++;frame(a,'convert',label,0,false,{sourceId,detail:`炼化 ${spent} 护盾`});}break;}case 'apply_status':if(a===p&&t===e&&curseTier>=1&&['weak','vulnerable'].includes(ef.status??''))n++;if(a===p&&t===e&&ef.status==='burn'&&burnTier>=2)n++;if(a===p&&t===e&&ef.status==='break'&&breakTier>=1)n++;t.status[ef.status!]=clamp((t.status[ef.status!]??0)+n,0,ef.status==='stun'?1:20);if(a===p&&t===e&&finisher.active&&!finisherUsed&&finisher.id==='burn'&&ef.status==='burn'&&(t.status.burn??0)>=8){finisherUsed=true;const stacks=t.status.burn??0,burst=stacks*4;t.status.burn=Math.max(0,stacks-3);hurt(p,e,burst,'焚天劫火',false,{base:burst,scaled:burst,afterDefense:burst,afterVulnerable:burst,afterBuild:burst,afterCrit:burst,absorbed:0,hpLoss:0},'build.finisher.burn');frame(undefined,'build','终局法相 · 焚天劫火',0,false,{detail:`爆燃 ${stacks} 层，消耗 3 层燃烧`});}if(a===p&&t===e&&finisher.active&&!finisherUsed&&finisher.id==='break'&&ef.status==='break'&&(t.status.break??0)>=6){finisherUsed=true;t.status.vulnerable=clamp((t.status.vulnerable??0)+2,0,20);const burst=Math.max(1,Math.floor(stat(p,'attack')*120/100));hurt(p,e,burst,'万剑决阵',false,{base:burst,scaled:burst,afterDefense:burst,afterVulnerable:burst,afterBuild:burst,afterCrit:burst,absorbed:0,hpLoss:0},'build.finisher.break');frame(undefined,'build','终局法相 · 万剑决阵',0,false,{detail:'破甲贯体，追加 2 层易伤'});}break;case 'remove_status':delete t.status[ef.status!];break;case 'modify_stat':{const k=ef.stat!;t.stats[k]=Math.max(k==='hp'?1:0,Math.floor(t.stats[k]*(100+value(ef.percent,rank))/100)+n);if(k==='hp')t.hp=Math.min(t.hp,t.stats.hp);break;}default:throw new Error('Non-combat effect in combat: '+ef.type);}return true;}
 function checkBossPhase(){const phases=enemy.phases??[];while(e.phase<phases.length&&e.hp*100<=e.stats.hp*phases[e.phase].hpPercent){const ph=phases[e.phase++];summary.phaseChanges++;frame(undefined,'phase',ph.label,0,false,{phase:e.phase,detail:`生命降至 ${ph.hpPercent}% 阈值`});for(const ef of ph.effects??[])apply(e,p,ef,0,ph.label,true,'boss.phase');}}
 function trigger(a:Fighter,b:Fighter,on:string){if(a.hp<=0)return;if(on==='on_heal'&&a.used.includes('healing-trigger'))return;if(on==='on_heal')a.used.push('healing-trigger');const list=[...a.abilities.map(eq=>({id:eq.id,rank:eq.rank,triggers:c.abilities.find(x=>x.id===eq.id)!.triggers})),...(a.fate?[{id:'fate',rank:0,triggers:a.fate.triggers??[]}]:[])];for(const item of list)for(let i=0;i<item.triggers.length;i++){const t=item.triggers[i],key=item.id+':'+i;if(t.on!==on||(t.once&&a.used.includes(key))||!(t.conditions??[]).every(q=>conditionOK(c,s,q,a,b)))continue;if(t.chance!==undefined&&random(s,stream(a,'trigger.'+on))*100>=t.chance)continue;if(t.once)a.used.push(key);for(const effect of t.effects){if(b.hp<=0&&effect.target!=='self')continue;apply(a,b,effect,item.rank,c.abilities.find(x=>x.id===item.id)?.name??a.fate?.name??on,true,item.id);}}if(on==='on_heal')a.used=a.used.filter(x=>x!=='healing-trigger');}
 function legalActions(a:Fighter){return a.abilities.map(eq=>({eq,ability:c.abilities.find(x=>x.id===eq.id)!})).filter(x=>['basic','rage'].includes(x.ability.slot)&&(a.cooldown[x.ability.id]??0)<=0&&(x.ability.slot!=='rage'||a.rage>=x.ability.cost));}
 function actionScore(x:{eq:Equipped;ability:Ability}){const hit=x.ability.effects.find(e=>e.type==='damage');return (x.ability.priority??0)+(x.ability.slot==='rage'?200:0)+value(hit?.coefficient,x.eq.rank)+value(hit?.value,x.eq.rank);}
 function chooseAction(a:Fighter){const all=legalActions(a);if(!all.length)return undefined;if(a===p){const rage=all.filter(x=>x.ability.slot==='rage').sort((x,y)=>actionScore(y)-actionScore(x));if(rage.length)return rage[0];return all.filter(x=>x.ability.slot==='basic').sort((x,y)=>actionScore(y)-actionScore(x))[0]??all[0];}if(enemy.ai==='weighted')return weighted(s,'combat.enemy_ai',all,x=>Math.max(1,actionScore(x)));return [...all].sort((x,y)=>actionScore(y)-actionScore(x)||x.ability.id.localeCompare(y.ability.id))[0];}
 function tickStatuses(a:Fighter,b:Fighter){let changed=false;if(a.status.burn&&a.hp>0){const perStack=c.rules.burnDamage+(a===e&&burnTier>=1?1:0),n=a.status.burn*perStack;a.status.burn=Math.max(0,a.status.burn-1);summary.statusTicks++;hurt(b,a,n,'燃烧',false,{base:n,scaled:n,afterDefense:n,afterVulnerable:n,afterBuild:n,afterCrit:n,absorbed:0,hpLoss:0},'status.burn');if(a===e&&p.hp>0&&bridgeTier('burn_guard')>0){const pct=bridgeTier('burn_guard')>=2?25:15,gain=Math.max(1,Math.floor(stat(p,'defense')*pct/100));p.shield=Math.min(9999,p.shield+gain);frame(undefined,'build','炉心金身',0,false,{detail:`劫火炼甲，护盾 +${gain}`});}changed=true;}for(const st of ['weak','break','vulnerable'])if(a.status[st]){a.status[st]=Math.max(0,a.status[st]-1);changed=true;}if(changed&&a.hp>0)frame(undefined,'status_tick',a.name+' 状态结算',0,false,{detail:'持续状态衰减'});}
 function objectiveReached(fullRound=false){if(p.hp<=0)return false;if(objective.type==='kill')return e.hp<=0;if(objective.type==='break')return e.hp<=0||objectiveWon;if(objective.type==='survive')return e.hp<=0||fullRound&&round>=(objective.rounds??1);return false;}
 if(evadeTier>=1)p.stats.dodge+=12;
 frame(undefined,'start','斗法开始',0,false,{objective:objective.label??objective.type});
 if(rageTier>=1)changeRage(p,15);if(guardTier>=1)p.shield=Math.min(9999,p.shield+Math.floor(stat(p,'defense')*.6));
 const active=build.resonances.filter(x=>x.tier>0).map(x=>`${x.name}·${x.tier===2?'二重':'一重'}`),fateBias=p.fate?.resonanceBias??[];if(fateBias.length)active.push(`命格引路·${fateBias.map(x=>schoolName(c,x)).join('/')}`);if(build.strategy.tier)active.push(`战策统摄 +${build.strategy.bonusPercent}%`);if(build.bridge.tier)active.push(`${build.bridge.name}·${build.bridge.tier===2?'二重':'一重'}`);if(build.finisher.active)active.push(`终局法相·${build.finisher.name}`);if(active.length)frame(undefined,'build','构筑共鸣',0,false,{detail:active.join(' / ')});
 const origin=c.origins.find(o=>o.id===s.origin);if(origin?.battleStartEffects?.length){for(const ef of origin.battleStartEffects)apply(p,e,ef,0,origin.name,false,`origin.${origin.id}`);frame(undefined,'origin',`出身 · ${origin.name}`,0,false,{sourceId:`origin.${origin.id}`,detail:origin.description});}trigger(p,e,'battle_start');trigger(e,p,'battle_start');frame(undefined,'ready','命格与法宝已生效');
 for(round=1;round<=c.rules.maxRounds&&p.hp>0&&e.hp>0&&!objectiveWon;round++){
  eventBudget=0;for(const a of [p,e])for(const id of Object.keys(a.cooldown))if(a.cooldown[id]>0)a.cooldown[id]--;
  const order=p.stats.speed>=e.stats.speed?[p,e]:[e,p];
  for(const a of order){const b=a===p?e:p;if(a.hp<=0||b.hp<=0||objectiveWon)break;if(a.status.stun){delete a.status.stun;frame(a,'stun','眩晕，跳过行动');continue;}trigger(a,b,'before_action');trigger(a,b,'hp_threshold');if(a.hp<=0||b.hp<=0)break;const picked=chooseAction(a);if(!picked){changeRage(a,5);frame(a,'wait','无可用主行动',0,false,{detail:'冷却或怒气条件未满足；怒气 +5'});continue;}const {eq,ability}=picked,useRage=ability.slot==='rage';if(useRage){changeRage(a,-ability.cost);if(a===p)summary.playerRageSkills++;else summary.enemyRageSkills++;}let hit=false;for(const ef of ability.effects){if(b.hp<=0&&ef.target!=='self')continue;const landed=apply(a,b,ef,eq.rank,ability.name,false,ability.id);if(ef.type==='damage'&&ef.target!=='self')hit=landed!==false;if(objectiveWon)break;}if(ability.cooldown)a.cooldown[ability.id]=ability.cooldown;if(ability.slot==='basic')changeRage(a,c.rules.basicRage);if(hit){changeRage(b,c.rules.hitRage);trigger(a,b,'on_hit');trigger(b,a,'on_damaged');trigger(b,a,'hp_threshold');}if(useRage){trigger(a,b,'on_rage_skill');if(a===p&&rageTier>=2)changeRage(a,18);if(a===p&&b.hp>0){const rb=bridgeTier('rage_burn');if(rb>0){const add=rb>=2?2:1;b.status.burn=clamp((b.status.burn??0)+add,0,20);frame(undefined,'build','雷火轮转',0,false,{detail:`怒气技引燃 +${add}`});if((b.status.burn??0)>=6){const burst=(b.status.burn??0)*(rb>=2?3:2);hurt(p,b,burst,'雷火劫伤',false,{base:burst,scaled:burst,afterDefense:burst,afterVulnerable:burst,afterBuild:burst,afterCrit:burst,absorbed:0,hpLoss:0},'build.bridge.rage_burn');}}const rbreak=bridgeTier('rage_break');if(rbreak>0&&(b.status.break??0)>0)changeRage(a,rbreak>=2?15:8);if(finisher.active&&!finisherUsed&&finisher.id==='rage'&&b.hp>0){finisherUsed=true;const burst=Math.max(1,Math.floor(stat(p,'attack')*90/100));hurt(p,b,burst,'九转雷劫',false,{base:burst,scaled:burst,afterDefense:burst,afterVulnerable:burst,afterBuild:burst,afterCrit:burst,absorbed:0,hpLoss:0},'build.finisher.rage');frame(undefined,'build','终局法相 · 九转雷劫',0,false,{detail:`怒气技引动雷劫追击 ${burst}`});}}}trigger(a,b,'after_action');trigger(a,b,'hp_threshold');if(a===p&&p.hp>0&&b.hp>0&&finisher.active&&finisher.id==='curse'&&!finisherUsed&&(b.status.weak??0)+(b.status.vulnerable??0)>=6){finisherUsed=true;apply(p,e,{type:'damage',coefficient:130,pure:true},0,'百厄归尘',true,'build.finisher.curse');}const notes:string[]=[];if(ability.cooldown)notes.push(`进入 ${ability.cooldown} 回合冷却`);if(a===p&&useRage&&rageTier>=2)notes.push('怒潮回气 +18');if(a===p&&useRage&&bridgeTier('rage_break')>0&&(b.status.break??0)>0)notes.push(`裂阵回潮 +${bridgeTier('rage_break')>=2?15:8} 怒气`);frame(a,useRage?'rage':'action',ability.name,0,false,{sourceId:ability.id,detail:notes.join('；')||undefined});if(objectiveReached()){objectiveWon=true;break;}}
  if(p.hp<=0||e.hp<=0||objectiveWon)break;for(const a of [p,e]){const b=a===p?e:p;trigger(a,b,'round_end');tickStatuses(a,b);if(a===p&&p.hp>0&&e.hp>0&&bridgeTier('vitality_curse')>0&&(e.status.weak??0)>0)apply(p,e,{type:'heal',target:'self',value:bridgeTier('vitality_curse')===2?4:2},0,'枯荣同契',true,'build.bridge.vitality_curse');trigger(a,b,'hp_threshold');if(p.hp<=0||e.hp<=0)break;}if(objectiveReached(true)){objectiveWon=true;break;}
 }
 const won=p.hp>0&&(objectiveWon||e.hp<=0);if(won)trigger(p,e,'battle_end');if(!won&&p.hp>0){p.hp=0;frame(undefined,'timeout','劫气耗尽',0,false,{detail:'未在规则允许的回合内完成目标'});}frame(undefined,won?'win':'loss',won?'斗法告捷':'此身入劫',0,false,{objective:objective.label??objective.type,detail:won?`目标完成：${objective.label??objective.type}`:`目标失败：${objective.label??objective.type}`});
 return {enemyId:enemy.id,frames,won,rounds:Math.min(round,c.rules.maxRounds),hp:p.hp,cursor:0,objective,summary,objectiveProgress};
}

export function selectRunPool(c:Content,s:RunState):RunPool {
 const o=c.origins.find(x=>x.id===s.origin)!,f=c.fates.find(x=>x.id===s.fate)!;
 const anchor=c.schools.find(x=>x.tags.includes(o.affinity))!.id;
 const required=new Set<SchoolId>([anchor,...(f.resonanceBias??[])]),candidates:SchoolId[][]=[];
 function visit(at:number,chosen:SchoolId[]){
  if(chosen.length===c.rules.poolSchools){if([...required].every(id=>chosen.includes(id))&&c.hybrids.filter(h=>h.schools.every(id=>chosen.includes(id))).length>=c.rules.poolHybrids)candidates.push(chosen);return;}
  for(let i=at;i<c.schools.length;i++)visit(i+1,[...chosen,c.schools[i].id]);
 }
 visit(0,[]);if(!candidates.length)throw new Error('No compatible run pool');
 const schools=pick(s,'content.schools',candidates),hybrids=sample(s,'content.hybrids',c.hybrids.filter(h=>h.schools.every(id=>schools.includes(id))),c.rules.poolHybrids).map(h=>h.id);
 return {schools,hybrids};
}
export function inRunPool(c:Content,s:RunState,a:Ability):boolean {
 if(s.slots.some(eq=>eq?.id===a.id))return true;
 if(a.hybrid)return s.contentPool.hybrids.includes(a.hybrid);
 return !a.schools?.length||a.schools.every(id=>s.contentPool.schools.includes(id));
}
export function routeLength(c:Content):number{return c.rules.routeRows.length;}

export class Game {
 c:Content;s:RunState;
 constructor(c:Content,s:RunState){this.c=c;this.s=s;}
 static create(c:Content,seed:string,origin:string,fate:string,name='无名',portrait=''):Game {
  validateContent(c);const o=c.origins.find(x=>x.id===origin);if(!o||!fateOptions(c,seed).some(x=>x.id===fate))throw new Error('Invalid origin or offered fate');
  const s:RunState={contentPool:{schools:[],hybrids:[]},version:c.version,rulesVersion:c.rulesVersion,seed,name:name.slice(0,14)||'无名',portrait:portrait||o.art,origin,fate,act:0,row:0,lane:1,phase:'map',hp:0,gold:c.rules.startingGold,xp:0,level:0,bonus:{},slots:SLOT_ORDER.map(()=>null),rng:{},route:[],visited:[],facts:{},counters:{mercy:0,defiance:0,chan_relation:0,jie_relation:0},threads:{},seenEvents:{},refreshes:[0,0,0,0],reward:[],stock:[],temper:[],actionLog:[],storyLog:[]};
  for(const id of o.starting){const a=c.abilities.find(x=>x.id===id)!;const index=SLOT_ORDER.findIndex((slot,i)=>slot===a.slot&&!s.slots[i]);s.slots[index]={id,rank:0};}s.hp=playerStats(c,s).hp;s.contentPool=selectRunPool(c,s);
  for(let act=0;act<c.acts.length;act++){
   const boss=pick(s,`route.boss.${act}`,c.acts[act].bosses??[c.acts[act].boss]);
   const bags:Record<string,string[]>={};
   for(let row=0;row<routeLength(c);row++){
    const types=c.rules.routeRows[row],order=types.length===3?sample(s,`route.${act}`,types,3):types;
    order.forEach((type,i)=>{
     const lane=order.length===1?1:i;let ref:string|undefined;
     if(['combat','elite'].includes(type)){
      if(!bags[type]?.length)bags[type]=sample(s,`route.encounter.${act}.${type}`,c.enemies.filter(e=>e.act===act&&e.kind===type).map(e=>e.id),99);
      ref=bags[type].pop();
     }
     if(type==='major')ref=c.acts[act].major;if(type==='boss')ref=boss;
     s.route.push({id:`${act}.${row}.${lane}`,act,row,lane,type,ref});
    });
   }
  }
  return new Game(c,s);
 }
 static load(c:Content,text:string):Game {
  if(text.length>4000000)throw new Error('Save too large');const s=JSON.parse(text) as RunState;if(s.version!==c.version||s.rulesVersion!==c.rulesVersion)throw new Error('Save/content version mismatch');if(!c.origins.some(o=>o.id===s.origin)||!c.fates.some(f=>f.id===s.fate)||typeof s.seed!=='string'||typeof s.name!=='string')throw new Error('Invalid identity');if(!Array.isArray(s.slots)||s.slots.length!==8||!s.slots[0]||!s.slots[1])throw new Error('Invalid slots');const seen=new Set<string>();s.slots.forEach((eq,i)=>{if(eq){const a=c.abilities.find(x=>x.id===eq.id);if(!a||a.slot!==SLOT_ORDER[i]||!Number.isInteger(eq.rank)||eq.rank<0||eq.rank>3||seen.has(eq.id))throw new Error('Invalid ability');seen.add(eq.id);}});for(const k of ['hp','gold','xp','level','act','row','lane'] as const)if(!Number.isInteger(s[k])||s[k]<0)throw new Error('Invalid '+k);if(s.act>=c.acts.length||s.row>=routeLength(c)||s.lane>2||s.level>=c.realms.length||!['map','battle','reward','shop','rest','event','event_result','ending','finished','dead'].includes(s.phase))throw new Error('Invalid progress');if(!s.contentPool||s.contentPool.schools.length!==c.rules.poolSchools||new Set(s.contentPool.schools).size!==c.rules.poolSchools||s.contentPool.schools.some(id=>!c.schools.some(d=>d.id===id))||s.contentPool.hybrids.length!==c.rules.poolHybrids||new Set(s.contentPool.hybrids).size!==c.rules.poolHybrids||s.contentPool.hybrids.some(id=>!c.hybrids.some(h=>h.id===id&&h.schools.every(k=>s.contentPool.schools.includes(k)))))throw new Error('Invalid saved pool');if(!s.rng||!s.facts||!s.counters||!s.threads||!s.seenEvents||!Array.isArray(s.route)||!Array.isArray(s.actionLog)||s.actionLog.length>10000)throw new Error('Invalid save shape');if(s.phase==='battle'&&(!s.battle||!c.enemies.some(e=>e.id===s.battle!.enemyId)||!Array.isArray(s.battle.frames)||s.battle.frames.length>4000))throw new Error('Invalid replay');if(s.pending&&!c.abilities.some(a=>a.id===s.pending!.id))throw new Error('Invalid pending reward');if(['event','event_result'].includes(s.phase)&&!c.events.some(e=>e.id===s.eventId))throw new Error('Invalid story');return new Game(c,s);
 }
 stats():Stats{return playerStats(this.c,this.s);}
 build():BuildProfile{return buildProfile(this.c,this.s);}
 role(id:string):AbilityRoleInfo{return abilityRole(this.c,this.s,this.ability(id));}
 ability(id:string):Ability {const a=this.c.abilities.find(a=>a.id===id);if(!a)throw new Error('Unknown ability');return a;}
 log(command:string,...args:any[]){this.s.actionLog.push({command,args});}
 available():RouteNode[]{const s=this.s;return s.phase==='map'?s.route.filter(n=>n.act===s.act&&n.row===s.row&&(s.row===0||Math.abs(n.lane-s.lane)<=1)):[];}
  resolveStory(n:RouteNode):Story {
   if(n.ref){const existing=this.c.events.find(e=>e.id===n.ref);if(!existing)throw new Error('Broken event reference');return existing;}
   const hidden=n.type==='hidden',base=this.c.events.filter(e=>e.act===n.act&&!e.major&&!!e.hiddenOnly===hidden&&(e.requirements??[]).every(q=>conditionOK(this.c,this.s,q)));if(!base.length)throw new Error(hidden?'No hidden event pool':'No visible event pool');
   let pool=base.filter(e=>(this.s.seenEvents[e.id]??0)<(e.maxPerRun??(e.profile==='B'?2:1)));if(!pool.length)pool=base;
   const chosen=weighted(this.s,`event.select.${n.id}`,pool,e=>{const relevant=(e.relevance?.length??0)>0&&(e.relevance??[]).every(q=>conditionOK(this.c,this.s,q)),fresh=(this.s.seenEvents[e.id]??0)===0;return Math.max(.1,(e.weight??1)*(relevant?4:1)*(fresh?1.25:.65));});
   n.ref=chosen.id;return chosen;
  }
 enter(id:string){const n=this.available().find(x=>x.id===id);if(!n)throw new Error('Node not reachable');const s=this.s;s.current=n;s.lane=n.lane;s.visited.push(n.id);s.pending=undefined;s.resultText=undefined;s.lastCheck=undefined;this.log('enter',id);if(['combat','elite','boss'].includes(n.type)){s.phase='battle';s.battle=simulateBattle(this.c,s,this.c.enemies.find(e=>e.id===n.ref)!);}else if(n.type==='event'||n.type==='major'||n.type==='hidden'){s.phase='event';const ev=n.type==='major'?this.c.events.find(e=>e.id===n.ref)!:this.resolveStory(n);s.eventId=ev.id;const starts=(ev.starts??[]).filter(x=>(x.conditions??[]).every(q=>conditionOK(this.c,s,q)));s.eventPhase=starts.length?weighted(s,`event.entry.${n.id}`,starts,x=>x.weight).phase:ev.startPhase??ev.phases?.[0]?.id;s.eventStep=0;s.seenEvents[ev.id]=(s.seenEvents[ev.id]??0)+1;}else if(n.type==='treasure'){s.phase='reward';s.rewardInfo={gold:0,xp:0};s.reward=this.rollAbilities(3,'treasure','artifact');}else if(n.type==='shop'){s.phase='shop';this.rollShop();}else{s.phase='rest';s.temper=sample(s,'reward',STAT_KEYS,3);}}
 advance(){const s=this.s;s.pending=undefined;s.battle=undefined;s.reward=[];s.stock=[];s.resultText=undefined;s.lastCheck=undefined;s.eventId=undefined;s.eventPhase=undefined;s.eventStep=undefined;if(s.row===routeLength(this.c)-1){if(s.act===this.c.acts.length-1){s.phase='ending';return;}s.act++;s.row=0;s.lane=1;if(this.c.rules.actStartFullHeal)s.hp=this.stats().hp;}else{s.row++;}s.phase='map';s.current=undefined;}
 finishBattle(){const s=this.s;if(s.phase!=='battle'||!s.battle)throw new Error('No battle');this.log('finishBattle');s.hp=s.battle.hp;if(!s.battle.won){s.phase='dead';return;}const gain=this.c.rules.rewards[s.current!.type];s.gold+=gain.gold;this.gainXP(gain.xp);s.rewardInfo=clone(gain);s.phase='reward';s.reward=this.rollAbilities(3,'reward');}
 gainXP(amount:number){this.s.xp+=amount;while(this.s.level+1<this.c.rules.xpThresholds.length&&this.s.xp>=this.c.rules.xpThresholds[this.s.level+1])this.s.level++;}
 rollAbilities(n:number,streamName:string,slot?:Slot):string[]{
  const s=this.s,affinity=this.c.origins.find(o=>o.id===s.origin)!.affinity,build=this.build(),coreTags=build.core==='none'?[]:schoolTags(this.c,build.core),weakSchool=build.bridge.counts[0]<=build.bridge.counts[1]?build.bridge.schools[0]:build.bridge.schools[1],weakTags=schoolTags(this.c,weakSchool);let pool=this.c.abilities.filter(a=>a.rarity!=='mythic'&&inRunPool(this.c,s,a)&&(!slot||a.slot===slot)&&!s.slots.some(eq=>eq?.id===a.id&&eq.rank===3)),out:string[]=[];
  const common=(a:Ability)=>1+(a.tags.includes(affinity)?1:0)+a.tags.reduce((sum,t)=>sum+Math.min(2,build.tags[t]??0),0)+this.role(a.id).fit;
  const choose=(lane:'core'|'bridge'|'pivot',index:number)=>{let candidates=pool;if(lane==='core'){const focused=pool.filter(a=>{const r=this.role(a.id);return r.role==='core'||r.role==='amplifier'||r.role==='finisher'||a.tags.some(t=>coreTags.includes(t));});if(focused.length)candidates=focused;}else if(lane==='bridge'){const focused=pool.filter(a=>this.role(a.id).role==='bridge'||a.tags.some(t=>weakTags.includes(t)));if(focused.length)candidates=focused;}else{const focused=pool.filter(a=>!s.slots.some(eq=>eq?.id===a.id)&&(!a.tags.some(t=>coreTags.includes(t))||this.role(a.id).role==='starter'));if(focused.length)candidates=focused;}const picked=weighted(s,`${streamName}.${lane}.${index}`,candidates,a=>{const r=this.role(a.id),coreHit=a.tags.some(t=>coreTags.includes(t)),weakHit=a.tags.some(t=>weakTags.includes(t)),novel=a.tags.filter(t=>!(build.tags[t]??0)).length;if(lane==='core')return common(a)+(coreHit?5:0)+(r.role==='core'?4:r.role==='amplifier'?3:r.role==='finisher'?2:0);if(lane==='bridge')return common(a)+(weakHit?6:0)+(r.role==='bridge'?6:0);return 2+novel*3+(r.role==='starter'?2:0)+(a.rarity==='rare'?1:0);});out.push(picked.id);pool=pool.filter(x=>x.id!==picked.id);};
  if(n>=3&&!slot){choose('core',0);if(pool.length)choose('bridge',1);if(pool.length)choose('pivot',2);while(pool.length&&out.length<n)choose(out.length%2?'bridge':'core',out.length);}else while(pool.length&&out.length<n){const picked=weighted(s,`${streamName}.fit.${out.length}`,pool,a=>common(a)+(a.tags.some(t=>coreTags.includes(t))?2:0)+(build.bridge.tier<2&&a.tags.some(t=>weakTags.includes(t))?2:0));out.push(picked.id);pool=pool.filter(x=>x.id!==picked.id);}return out;
 }
 acquire(id:string,source:Pending['source'],price=0,index?:number){const s=this.s;if(s.pending)throw new Error('Resolve existing replacement');if(s.gold<price)throw new Error('Not enough currency');const a=this.ability(id),duplicate=s.slots.findIndex(eq=>eq?.id===id);if(duplicate>=0){if(s.slots[duplicate]!.rank>=3)throw new Error('Maximum rank');s.slots[duplicate]!.rank++;this.completeAcquire({id,source,price,index});return;}const empty=SLOT_ORDER.findIndex((slot,i)=>slot===a.slot&&!s.slots[i]);if(empty>=0){s.slots[empty]={id,rank:0};this.completeAcquire({id,source,price,index});return;}s.pending={id,source,price,index};}
 completeAcquire(pending:Pending){const s=this.s;s.gold-=pending.price;if(pending.source==='shop')s.stock[pending.index!].sold=true;s.hp=Math.min(s.hp,this.stats().hp);s.pending=undefined;if(pending.source==='reward')this.advance();}
 replace(index:number){const p=this.s.pending;if(!p||SLOT_ORDER[index]!==this.ability(p.id).slot||!this.s.slots[index])throw new Error('Invalid replacement');if(this.s.gold<p.price)throw new Error('Not enough currency');this.log('replace',index);this.s.slots[index]={id:p.id,rank:0};this.completeAcquire(p);}
 cancelReplacement(){if(!this.s.pending)throw new Error('No replacement');this.log('cancelReplacement');this.s.pending=undefined;}
 reward(index:number){if(this.s.phase!=='reward'||!this.s.reward[index]||this.s.pending)throw new Error('No reward');this.log('reward',index);this.acquire(this.s.reward[index],'reward');}
 skipReward(){if(this.s.phase!=='reward'||this.s.pending)throw new Error('No reward');this.log('skipReward');this.advance();}
 rollShop(){const s=this.s;s.stock=this.rollAbilities(3,'shop').map(id=>({kind:'ability' as const,id,price:this.ability(id).rarity==='rare'?55:35,sold:false}));s.stock.push({kind:'heal',price:25,value:30,sold:false});const stat=pick(s,'shop.stat',STAT_KEYS.filter(x=>x!=='hp'));s.stock.push({kind:'stat',price:45,stat,value:stat==='attack'?2:1,sold:false});}
 buy(index:number){const s=this.s,offer=s.stock[index];if(s.phase!=='shop'||s.pending||!offer||offer.sold||s.gold<offer.price)throw new Error('Cannot buy');this.log('buy',index);if(offer.kind==='ability')this.acquire(offer.id!,'shop',offer.price,index);else if(offer.kind==='heal'){s.gold-=offer.price;s.hp=Math.min(this.stats().hp,s.hp+Math.floor(this.stats().hp*(offer.value??30)/100));offer.sold=true;}else{s.gold-=offer.price;s.bonus[offer.stat!]=(s.bonus[offer.stat!]??0)+(offer.value??1);offer.sold=true;}}
 refresh(){const s=this.s,n=s.refreshes[s.act],price=this.c.rules.refreshPrices[n];if(s.phase!=='shop'||s.pending||price===undefined||s.gold<price)throw new Error('Cannot refresh');this.log('refresh');s.gold-=price;s.refreshes[s.act]++;this.rollShop();}
 leaveShop(){if(this.s.phase!=='shop'||this.s.pending)throw new Error('Not in shop');this.log('leaveShop');this.advance();}
 rest(kind:string,index?:number){const s=this.s;if(s.phase!=='rest')throw new Error('Not resting');if(kind==='heal')s.hp=Math.min(this.stats().hp,s.hp+Math.floor(this.stats().hp*this.c.rules.restPercent/100));else if(kind==='temper'){const stat=s.temper[index??-1];if(!stat)throw new Error('Invalid temper');s.bonus[stat]=(s.bonus[stat]??0)+(stat==='hp'?12:stat==='attack'?3:2);}else{const eq=s.slots[index??-1];if(!eq||eq.rank>=3||((this.ability(eq.id).slot==='artifact')!==(kind==='artifact')))throw new Error('Cannot rank up');eq.rank++;}this.log('rest',kind,index);this.advance();}
 applyWorld(ef:Effect){const s=this.s,n=value(ef.value);switch(ef.type){case 'heal':s.hp=Math.min(this.stats().hp,s.hp+n+Math.floor(this.stats().hp*(ef.maxHpPercent??0)/100));break;case 'damage':s.hp=Math.max(0,s.hp-n-Math.floor(this.stats().hp*(ef.maxHpPercent??0)/100));break;case 'gain_currency':s.gold=Math.max(0,s.gold+n);break;case 'gain_xp':this.gainXP(n);break;case 'modify_stat':s.bonus[ef.stat!]=(s.bonus[ef.stat!]??0)+n+Math.floor(this.stats()[ef.stat!]*value(ef.percent)/100);break;case 'set_fact':s.facts[ef.key!]=!!ef.value;break;case 'add_counter':s.counters[ef.key!]=(s.counters[ef.key!]??0)+n;break;case 'advance_thread':s.threads[ef.key!]=String(ef.value);break;case 'rank_up':{const eq=s.slots.find(x=>x?.id===ef.key);if(eq)eq.rank=Math.min(3,eq.rank+Math.max(1,n));break;}default:throw new Error('Non-world effect in event: '+ef.type);}}
 currentEvent():Story {const e=this.c.events.find(x=>x.id===this.s.eventId);if(!e)throw new Error('No event');return e;}
 eventPhase():EventPhase {const event=this.currentEvent();if(!event.phases?.length)return {id:'root',text:event.text,choices:event.choices};const id=this.s.eventPhase??event.startPhase??event.phases[0].id;const phase=event.phases.find(p=>p.id===id);if(!phase)throw new Error('Invalid event phase');return phase;}
 checkChance(choice:Choice):number|undefined {if(!choice.check)return undefined;const st=this.stats();return clamp(Math.round(choice.check.base+(st[choice.check.stat]-choice.check.difficulty)*4+st.luck/5),10,95);}
  conditionLabel(q:Condition):string {switch(q.type){case 'currency_at_least':return `需要 ${q.value??0} 灵石`;case 'hp_above_absolute':return `需要生命高于 ${q.value??0}`;case 'has_fact':return `需要因果 · ${this.c.labels.facts?.[q.key??'']??'旧事未成'}`;case 'counter_at_least':return `需要 ${this.c.labels.counters?.[q.key??'']??q.key} ≥ ${q.value??0}`;case 'stat_at_least':return `需要 ${this.c.labels.stats?.[q.stat??'luck']??q.stat} ≥ ${q.value??0}`;case 'thread_is':return '需要另一条旧因';case 'event_seen':return `需要先经历 ${this.c.events.find(e=>e.id===q.key)?.name??'前缘'}`;case 'act_is':return `仅在第 ${(q.value??0)+1} 幕可选`;default:return '条件未达成';}}
  eventChoices(){const event=this.currentEvent(),phase=this.eventPhase();return phase.choices.map(choice=>{const unmet=(choice.conditions??[]).filter(q=>!conditionOK(this.c,this.s,q)),legal=unmet.length===0;return {choice,legal,lockReason:legal?undefined:unmet.map(q=>this.conditionLabel(q)).join(' · '),chance:this.checkChance(choice),stat:choice.check?.stat,eventId:event.id,phase:phase.id};});}
 chooseEvent(id:string){const s=this.s;if(s.phase!=='event'||s.pending)throw new Error('No event');const event=this.currentEvent(),phase=this.eventPhase(),info=this.eventChoices().find(x=>x.choice.id===id);if(!info||!info.legal)throw new Error('Choice locked');const choice=info.choice;this.log('chooseEvent',id);let result=choice.text,next=choice.next,grant=choice.grant,chance:number|undefined,roll:number|undefined,success:boolean|undefined;if(choice.check){chance=info.chance!;roll=Math.floor(random(s,`event.check.${event.id}.${phase.id}.${choice.id}`)*100)+1;success=roll<=chance;const branch=success?choice.check.success:choice.check.failure;for(const ef of branch.effects??[])this.applyWorld(ef);result=branch.text;next=branch.next;grant=branch.grant;s.lastCheck={eventId:event.id,phase:phase.id,choiceId:choice.id,stat:choice.check.stat,chance,roll,success};}else for(const ef of choice.effects??[])this.applyWorld(ef);s.storyLog.push({act:s.act,title:event.name,choice:choice.label,phase:phase.id,result,chance,roll,success,eventId:event.id,choiceId:choice.id,category:event.category,consequence:choice.consequence});s.resultText=result;if(s.hp<=0){s.phase='dead';return;}if(next){if((s.eventStep??0)>=4)throw new Error('Event phase budget exceeded');s.eventPhase=next;s.eventStep=(s.eventStep??0)+1;s.phase='event';}else s.phase='event_result';if(grant){const ability=grant.id??this.rollAbilities(1,'event.reward',grant.slot)[0];if(ability)this.acquire(ability,'event');}}
 leaveEvent(){if(this.s.phase!=='event_result'||this.s.pending)throw new Error('Unresolved event');this.log('leaveEvent');this.advance();}
 legalEndings():Ending[]{return this.c.endings.filter(e=>e.conditions.every(q=>conditionOK(this.c,this.s,q)));}
 chooseEnding(id:string){if(this.s.phase!=='ending'||!this.legalEndings().some(e=>e.id===id))throw new Error('Ending unavailable');this.log('chooseEnding',id);this.s.endingId=id;this.s.phase='finished';}
 command(command:string,args:any[]){const actions:Record<string,(...a:any[])=>void>={enter:this.enter,finishBattle:this.finishBattle,reward:this.reward,skipReward:this.skipReward,replace:this.replace,cancelReplacement:this.cancelReplacement,buy:this.buy,refresh:this.refresh,leaveShop:this.leaveShop,rest:this.rest,chooseEvent:this.chooseEvent,leaveEvent:this.leaveEvent,chooseEnding:this.chooseEnding};if(!actions[command])throw new Error('Invalid command');actions[command].apply(this,args);}
}
export function replayRun(c:Content,source:RunState):RunState {const g=Game.create(c,source.seed,source.origin,source.fate,source.name,source.portrait);for(const a of source.actionLog)g.command(a.command,a.args);return g.s;}
